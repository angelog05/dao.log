---
// Post pages are now handled client-side via src/pages/404.astro
// Empty getStaticPaths() → no static HTML generated at build time
// All /posts/{slug} requests fall through to 404.html on GitHub Pages
export function getStaticPaths() {
  return []
}
---
